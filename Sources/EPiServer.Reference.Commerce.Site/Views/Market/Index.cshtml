@using EPiServer.Web.Routing
@model EPiServer.Reference.Commerce.Site.Features.Market.Models.MarketViewModel
@using (Html.BeginForm("Set", "Market", new { contentLink = @Model.ContentLink }))
{
    @Html.DropDownListFor(x => x.MarketId, Model.Markets, new { @class = "form-control input-sm jsMarketSelector" })
}